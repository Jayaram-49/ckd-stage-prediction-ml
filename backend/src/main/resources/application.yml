server:
  port: ${SERVER_PORT:8080} # dynamic port: use SERVER_PORT env var, default 8080

spring:
  application:
    name: ckd-ai-backend
  datasource:
    url: jdbc:mysql://localhost:3306/ckd_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: Jaya@2507
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    defer-datasource-initialization: true
  sql:
    init:
      mode: always
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME:your-email@gmail.com}
    password: ${MAIL_PASSWORD:your-app-password}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

jwt:
  secret: 404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
  expiration: 86400000 # 24 hours

ml-service:
  url: http://localhost:8000

logging:
  level:
    org.springframework.security: DEBUG
